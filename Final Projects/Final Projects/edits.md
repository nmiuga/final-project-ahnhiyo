#  Edits

1. Build a multi-view SwiftUI restaurant discovery app (“What Are We Eating?”) with a cinematic magazine-style design and a bottom tab bar.
2. Integrate Google Places live data (search/details/photos), parse the required fields, calculate distance, load images, and show proper loading/empty/error states.
3. Improve the home experience from “basic like Maps” into something more interactive/magazine-like (later pivoting away from a strict quiz while keeping the magazine vibe).
4. Fix layout issues: content going off-screen, too high up, and alignment/centering problems across screens.
5. Remove the “weird gradient/vignette” background and make styling more consistent.
6. Add party-size selection (“who’s coming” / how many people).
7. Make “Surprise” the main flow: open app → generate a random nearby pick using location.
8. Fix location-permission UI: pop-up/button styling, equal button sizing, and hide/replace the enable button when permission is already granted.
9. Make cards/images consistent in size (stop stretching/shifting; photo height should not change on shuffle).
10. Make headings/cover text more dynamic (not repeating the same words) and overall UI more “fun.”
11. Fix Search page: restore the earlier filter/questions layout, align it correctly, and make it functionally work.
12. Diagnose “No restaurants found” / API key issues and improve/expand how Google APIs are used so search actually returns places.
13. Fix the second page not being scrollable.
14. Apply imported fonts (Alata + Cal Sans) everywhere so every word uses one of those fonts.
